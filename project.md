# 需求说明：基于本地大模型构建“Word 需求转 XMind 测试导图”的 Python 自动化工具

你好！我需要你帮我编写一个纯本地化运行的 Python 脚本。
业务场景是：将结构化的《软件需求规格说明书》（Word格式）自动解析，并通过本地部署的 Ollama 大模型进行逻辑推理，最终生成标准化的测试用例思维导图（XMind格式）。
该工具必须完全离线运行，不允许调用任何外部网络 API。

## 一、 核心组件与库依赖
1. **文档解析**：使用 `python-docx` 库。
2. **大模型调用**：使用 `ollama` Python 库连接本地服务。模型指定使用 `qwen3.5:4b`
3. **思维导图生成**：使用 `xmind` 或 `xmindparser` 库（建议生成兼容性高的 `.xmind` 格式文件）。

## 二、 核心处理流程（Pipeline）

请按照以下五个阶段来设计代码架构：

### 阶段 1：Word 文档结构化读取与分块 (Chunking)
- **要求**：不能简单粗暴地提取纯文本。必须利用 `docx.Document` 解析段落样式（`paragraph.style.name`）。
- **逻辑**：识别“Heading 1”（一级标题）、“Heading 2”（二级标题）。将处于同一二级标题下的所有正文内容（功能描述、流程、异常处理等）拼接成一个**独立的文本块（Chunk）**。
- **目的**：利用“按业务模块分块”的策略，防止一次性输入字数过多导致本地大模型的上下文窗口（Context Window）溢出和截断。

### 阶段 2：构建 Prompt 模板
- **要求**：设计一个严谨的 Prompt 字符串模板，通过占位符 `{requirement_text}` 动态填入阶段1切分出来的文本块。
- **Prompt 核心要求**：
  1. 设定 AI 角色为资深测试工程师。
  2. 要求使用等价类、边界值和状态转移等方法挖掘测试点。
  3. **必须强制要求 AI 仅输出纯正的 JSON 字符串**，不允许包含任何 Markdown 代码块包裹（如 ```json ... ```），也不允许有解释性废话。

- **预期的目标 JSON 数据结构参考**：
```json
{
  "module_name": "（提取自传入的标题名）",
  "test_objects": [
    {
      "object_name": "测试对象名称",
      "req_id": "需求编号（若有）",
      "test_points": [
        {
          "point_name": "测试点（验证点）",
          "inputs_and_steps": "前置条件、输入及操作步骤",
          "expected_result": "预期结果与验证标准"
        }
      ]
    }
  ]
}
```

### 阶段 3：本地 Ollama 推理调用
- **要求**：编写一个调用本地 Ollama API 的函数。
- **参数控制**：设置 `temperature` 参数较低（如 `0.1` 或 `0`），保证逻辑严谨性；若模型支持 JSON 模式（如传入 `format="json"` 参数），请配置以降低解析错误率。
- **流控**：遍历阶段1生成的各个业务文本块，**逐个**发送给 Ollama。

### 阶段 4：JSON 解析与异常处理
- **要求**：接收到 AI 返回的字符串后，尝试使用 `json.loads` 解析。
- **容错机制**：如果 AI 不听话输出了 Markdown 标记，需编写一段简单的正则清理代码（剥离 ```json 和 ```）再进行反序列化。如果解析失败，记录日志并跳过当前模块。

### 阶段 5：生成 XMind 文件
- **要求**：创建一个新的 XMind 工作簿。
- **层级映射关系**：
  - **中心主题**：Word 文档的标题名称。
  - **一级节点**：对应 JSON 中的 `module_name`。
  - **二级节点**：对应 JSON 中的 `object_name`。
  - **三级节点**：对应 JSON 中的 `point_name`。
  - **四级节点（叶子节点）**：将 `inputs_and_steps` 和 `expected_result` 组合成文本写入。
- **保存**：将构建好的 XMind 对象保存到本地磁盘，路径用占位符代替。

## 三、 代码输出要求
1. 请提供**完整、可直接运行**的 Python 代码。
2. 代码必须有详细的中文注释，解释核心步骤的逻辑。
3. 请使用模块化设计，将上述 5 个阶段分别封装成独立的函数，最后在 `main()` 函数中串联。
4. 请在代码开头注明需要 `pip install` 的依赖库名称。

感谢！请开始编写代码。